{
  "name": "builderman",
  "scripts": {
    "dev": "pnpm --filter ./packages/* run -r dev",
    "build": "pnpm --filter ./packages/* run -r build",
    "test": "pnpm --filter ./packages/* run -r test"
  },
  "packageManager": "pnpm@10.20.0"
}
